# Work Packet Record — WP-0.1

## Identity

- Work Packet ID: `WP-0.1`
- Lot: `0`
- Name: Reproducible TypeScript/Vite bootstrap
- State: `REVIEW_PENDING`
- Current pass: `B-ADVERSARIAL-REVIEW`
- Primary bounded context: engineering bootstrap/toolchain
- Branch/PR: `lot-0/repository-tooling`

## Scope

### Primary Feature IDs

- None. Lot 0 is cross-cutting engineering foundation work and must not mark product Features implemented.

### Current-lot responsibilities covered

- framework-light Vite skeleton;
- strict TypeScript;
- npm package metadata + reproducible lockfile;
- stable path aliases;
- baseline build/boot;
- core `dev`, `build`, `typecheck` command contracts.

### Governing contracts

- `docs/roadmap/LOT-ACCEPTANCE.md` — Lot 0
- `docs/roadmap/LOTS.md` — Lot 0
- `docs/engineering/CODEBASE-STRUCTURE.md`
- `docs/engineering/CODING-STANDARDS.md`
- `docs/engineering/AI-LOT-ORCHESTRATION.md`
- `docs/quality/QUALITY-GATES.md`
- `docs/engineering/CI-CD.md`

### Explicitly out of scope

- lint/static architecture enforcement beyond bootstrap hooks (WP-0.2);
- full test harnesses (WP-0.3);
- Supabase local foundation (WP-0.4);
- permanent CI/preview/security workflows (WP-0.5);
- Lot integration/acceptance (WP-0.6);
- all product Feature implementation.

## Dependency / sequencing

- Required prior packets/features: none
- Downstream packets blocked by this packet: WP-0.2, WP-0.3, WP-0.4, WP-0.5
- Shared interfaces/contracts relied on: canonical roots and aliases from `CODEBASE-STRUCTURE.md`

## Sizing review

Planning complexity: **6/10 — PASS**.

## Expected vertical slice

- UI/route: minimal framework-light engineering bootstrap page only
- application/domain/ports/infrastructure: none yet
- UX impact: semantic bootstrap page only, explicitly states no business features exist

## Pass A — IMPLEMENT

### Implementation evidence

- `package.json` with exact dev dependency versions and Node/npm engine contract;
- committed npm lockfile v3 generated by GitHub Actions;
- `.nvmrc` → Node 24.20.0 LTS;
- `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json` strict project configuration;
- stable aliases `@app`, `@ui`, `@domain`, `@application`, `@infra`, `@shared`;
- framework-light `vite.config.ts`;
- minimal `index.html`, `src/main.ts`, bootstrap renderer and styles;
- temporary WP-0.1 GitHub Action proving a clean install/typecheck/build.

Runtime findings remediated during Pass A:

1. TypeScript 7.0.2 conflicted with the current `typescript-eslint` peer range. Resolution: use TypeScript 6.0.3; no `--force` or legacy peer-deps bypass.
2. TypeScript 6 rejected deprecated `baseUrl`. Resolution: remove `baseUrl` and keep relative `paths`; no deprecation suppression.
3. Strict TypeScript could not type the CSS side-effect import. Resolution: add `vite/client` types.

Latest verification run: GitHub Actions run `33794445680` reached clean `npm ci`, strict typecheck and production build successfully before job cleanup.

### Pass A exit

- [x] intended bootstrap exists
- [x] strict typecheck command exists
- [x] production build command exists
- [x] package lock committed and clean `npm ci` succeeds
- [x] no known untracked stub/TODO
- [x] packet moved to `REVIEW_PENDING`
- [x] current/next pass recorded as `B-ADVERSARIAL-REVIEW`

## Pass B — ADVERSARIAL REVIEW

In progress. Review reconstructs expectations from the governing contracts rather than Pass A's conclusions.

Review targets:
- framework/ADR drift;
- dependency/version compatibility;
- false strictness or deprecation suppression;
- non-reproducible install behavior;
- alias/layer bypass introduced by bootstrap;
- unsafe DOM rendering;
- production feature leakage into Lot 0;
- excessive workflow permissions or temporary bootstrap artifacts that could survive the Lot accidentally.

## Pass C — ACCEPTANCE / RECONCILIATION

Not started.

## Handoff

- Current state/pass: `REVIEW_PENDING / B-ADVERSARIAL-REVIEW`
- Last green verification: clean npm install + strict TypeScript + Vite production build
- Remaining blocker/finding: none open at Pass B start
- Next permitted action: finish adversarial review; if clean, transition to `ACCEPTANCE_PENDING`
